/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/OpDesc.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/func_unit.hh"

#include "enums/OpClass.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_OpDesc");
    py::class_<OpDescParams, SimObjectParams, std::unique_ptr<OpDescParams, py::nodelete>>(m, "OpDescParams")
        .def(py::init<>())
        .def("create", &OpDescParams::create)
        .def_readwrite("opClass", &OpDescParams::opClass)
        .def_readwrite("opLat", &OpDescParams::opLat)
        .def_readwrite("pipelined", &OpDescParams::pipelined)
        ;

    py::class_<gem5::OpDesc, gem5::SimObject, std::unique_ptr<gem5::OpDesc, py::nodelete>>(m, "gem5_COLONS_OpDesc")
        ;

}

static EmbeddedPyBind embed_obj("OpDesc", module_init, "SimObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyOpDescParamsClass
{
  public:
    gem5::OpDesc *create() const;
};

template <class CxxClass, class Enable=void>
class DummyOpDescShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyOpDescShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const OpDescParams &>>>
{
  public:
    using Params = OpDescParams;
    static gem5::OpDesc *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyOpDescShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const OpDescParams &>>>
{
  public:
    using Params = DummyOpDescParamsClass;
    static gem5::OpDesc *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::OpDesc *
DummyOpDescShunt<gem5::OpDesc>::Params::create() const
{
    return DummyOpDescShunt<gem5::OpDesc>::create(*this);
}

} // namespace gem5
