/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/X86FsWorkload.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/x86/fs_workload.hh"

#include "arch/x86/bios/acpi.hh"
#include "arch/x86/bios/intelmp.hh"
#include "arch/x86/bios/intelmp.hh"
#include "arch/x86/bios/smbios.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_X86FsWorkload");
    py::class_<X86FsWorkloadParams, KernelWorkloadParams, std::unique_ptr<X86FsWorkloadParams, py::nodelete>>(m, "X86FsWorkloadParams")
        .def(py::init<>())
        .def("create", &X86FsWorkloadParams::create)
        .def_readwrite("acpi_description_table_pointer", &X86FsWorkloadParams::acpi_description_table_pointer)
        .def_readwrite("intel_mp_pointer", &X86FsWorkloadParams::intel_mp_pointer)
        .def_readwrite("intel_mp_table", &X86FsWorkloadParams::intel_mp_table)
        .def_readwrite("smbios_table", &X86FsWorkloadParams::smbios_table)
        ;

    py::class_<gem5::X86ISA::FsWorkload, gem5::KernelWorkload, std::unique_ptr<gem5::X86ISA::FsWorkload, py::nodelete>>(m, "gem5_COLONS_X86ISA_COLONS_FsWorkload")
        ;

}

static EmbeddedPyBind embed_obj("X86FsWorkload", module_init, "KernelWorkload");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyX86FsWorkloadParamsClass
{
  public:
    gem5::X86ISA::FsWorkload *create() const;
};

template <class CxxClass, class Enable=void>
class DummyX86FsWorkloadShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyX86FsWorkloadShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const X86FsWorkloadParams &>>>
{
  public:
    using Params = X86FsWorkloadParams;
    static gem5::X86ISA::FsWorkload *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyX86FsWorkloadShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const X86FsWorkloadParams &>>>
{
  public:
    using Params = DummyX86FsWorkloadParamsClass;
    static gem5::X86ISA::FsWorkload *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::X86ISA::FsWorkload *
DummyX86FsWorkloadShunt<gem5::X86ISA::FsWorkload>::Params::create() const
{
    return DummyX86FsWorkloadShunt<gem5::X86ISA::FsWorkload>::create(*this);
}

} // namespace gem5
