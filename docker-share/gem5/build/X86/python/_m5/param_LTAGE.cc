/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/LTAGE.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/pred/ltage.hh"

#include "cpu/pred/loop_predictor.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_LTAGE");
    py::class_<LTAGEParams, TAGEParams, std::unique_ptr<LTAGEParams, py::nodelete>>(m, "LTAGEParams")
        .def(py::init<>())
        .def("create", &LTAGEParams::create)
        .def_readwrite("loop_predictor", &LTAGEParams::loop_predictor)
        ;

    py::class_<gem5::branch_prediction::LTAGE, gem5::branch_prediction::TAGE, std::unique_ptr<gem5::branch_prediction::LTAGE, py::nodelete>>(m, "gem5_COLONS_branch_prediction_COLONS_LTAGE")
        ;

}

static EmbeddedPyBind embed_obj("LTAGE", module_init, "TAGE");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyLTAGEParamsClass
{
  public:
    gem5::branch_prediction::LTAGE *create() const;
};

template <class CxxClass, class Enable=void>
class DummyLTAGEShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyLTAGEShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const LTAGEParams &>>>
{
  public:
    using Params = LTAGEParams;
    static gem5::branch_prediction::LTAGE *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyLTAGEShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const LTAGEParams &>>>
{
  public:
    using Params = DummyLTAGEParamsClass;
    static gem5::branch_prediction::LTAGE *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::branch_prediction::LTAGE *
DummyLTAGEShunt<gem5::branch_prediction::LTAGE>::Params::create() const
{
    return DummyLTAGEShunt<gem5::branch_prediction::LTAGE>::create(*this);
}

} // namespace gem5
