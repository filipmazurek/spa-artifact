/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/BasicRouter.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/ruby/network/BasicRouter.hh"

#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_BasicRouter");
    py::class_<BasicRouterParams, ClockedObjectParams, std::unique_ptr<BasicRouterParams, py::nodelete>>(m, "BasicRouterParams")
        .def(py::init<>())
        .def("create", &BasicRouterParams::create)
        .def_readwrite("latency", &BasicRouterParams::latency)
        .def_readwrite("router_id", &BasicRouterParams::router_id)
        ;

    py::class_<gem5::ruby::BasicRouter, gem5::ClockedObject, std::unique_ptr<gem5::ruby::BasicRouter, py::nodelete>>(m, "gem5_COLONS_ruby_COLONS_BasicRouter")
        ;

}

static EmbeddedPyBind embed_obj("BasicRouter", module_init, "ClockedObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyBasicRouterParamsClass
{
  public:
    gem5::ruby::BasicRouter *create() const;
};

template <class CxxClass, class Enable=void>
class DummyBasicRouterShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyBasicRouterShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const BasicRouterParams &>>>
{
  public:
    using Params = BasicRouterParams;
    static gem5::ruby::BasicRouter *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyBasicRouterShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const BasicRouterParams &>>>
{
  public:
    using Params = DummyBasicRouterParamsClass;
    static gem5::ruby::BasicRouter *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ruby::BasicRouter *
DummyBasicRouterShunt<gem5::ruby::BasicRouter>::Params::create() const
{
    return DummyBasicRouterShunt<gem5::ruby::BasicRouter>::create(*this);
}

} // namespace gem5
