/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/MemFootprintProbe.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/probes/mem_footprint.hh"

#include "base/types.hh"
#include "sim/system.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_MemFootprintProbe");
    py::class_<MemFootprintProbeParams, BaseMemProbeParams, std::unique_ptr<MemFootprintProbeParams, py::nodelete>>(m, "MemFootprintProbeParams")
        .def(py::init<>())
        .def("create", &MemFootprintProbeParams::create)
        .def_readwrite("page_size", &MemFootprintProbeParams::page_size)
        .def_readwrite("system", &MemFootprintProbeParams::system)
        ;

    py::class_<gem5::MemFootprintProbe, gem5::BaseMemProbe, std::unique_ptr<gem5::MemFootprintProbe, py::nodelete>>(m, "gem5_COLONS_MemFootprintProbe")
        ;

}

static EmbeddedPyBind embed_obj("MemFootprintProbe", module_init, "BaseMemProbe");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyMemFootprintProbeParamsClass
{
  public:
    gem5::MemFootprintProbe *create() const;
};

template <class CxxClass, class Enable=void>
class DummyMemFootprintProbeShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyMemFootprintProbeShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const MemFootprintProbeParams &>>>
{
  public:
    using Params = MemFootprintProbeParams;
    static gem5::MemFootprintProbe *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyMemFootprintProbeShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const MemFootprintProbeParams &>>>
{
  public:
    using Params = DummyMemFootprintProbeParamsClass;
    static gem5::MemFootprintProbe *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::MemFootprintProbe *
DummyMemFootprintProbeShunt<gem5::MemFootprintProbe>::Params::create() const
{
    return DummyMemFootprintProbeShunt<gem5::MemFootprintProbe>::create(*this);
}

} // namespace gem5
