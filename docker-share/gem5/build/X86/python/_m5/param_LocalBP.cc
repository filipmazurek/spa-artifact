/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/LocalBP.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/pred/2bit_local.hh"

#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_LocalBP");
    py::class_<LocalBPParams, BranchPredictorParams, std::unique_ptr<LocalBPParams, py::nodelete>>(m, "LocalBPParams")
        .def(py::init<>())
        .def("create", &LocalBPParams::create)
        .def_readwrite("localCtrBits", &LocalBPParams::localCtrBits)
        .def_readwrite("localPredictorSize", &LocalBPParams::localPredictorSize)
        ;

    py::class_<gem5::branch_prediction::LocalBP, gem5::branch_prediction::BPredUnit, std::unique_ptr<gem5::branch_prediction::LocalBP, py::nodelete>>(m, "gem5_COLONS_branch_prediction_COLONS_LocalBP")
        ;

}

static EmbeddedPyBind embed_obj("LocalBP", module_init, "BranchPredictor");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyLocalBPParamsClass
{
  public:
    gem5::branch_prediction::LocalBP *create() const;
};

template <class CxxClass, class Enable=void>
class DummyLocalBPShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyLocalBPShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const LocalBPParams &>>>
{
  public:
    using Params = LocalBPParams;
    static gem5::branch_prediction::LocalBP *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyLocalBPShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const LocalBPParams &>>>
{
  public:
    using Params = DummyLocalBPParamsClass;
    static gem5::branch_prediction::LocalBP *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::branch_prediction::LocalBP *
DummyLocalBPShunt<gem5::branch_prediction::LocalBP>::Params::create() const
{
    return DummyLocalBPShunt<gem5::branch_prediction::LocalBP>::create(*this);
}

} // namespace gem5
