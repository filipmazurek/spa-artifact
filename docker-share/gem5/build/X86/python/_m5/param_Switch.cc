/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/Switch.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/ruby/network/simple/Switch.hh"

#include "base/types.hh"
#include "base/types.hh"
#include <vector>
#include "mem/ruby/network/MessageBuffer.hh"
#include "mem/ruby/network/simple/routing/BaseRoutingUnit.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_Switch");
    py::class_<SwitchParams, BasicRouterParams, std::unique_ptr<SwitchParams, py::nodelete>>(m, "SwitchParams")
        .def(py::init<>())
        .def("create", &SwitchParams::create)
        .def_readwrite("ext_routing_latency", &SwitchParams::ext_routing_latency)
        .def_readwrite("int_routing_latency", &SwitchParams::int_routing_latency)
        .def_readwrite("port_buffers", &SwitchParams::port_buffers)
        .def_readwrite("routing_unit", &SwitchParams::routing_unit)
        .def_readwrite("virt_nets", &SwitchParams::virt_nets)
        ;

    py::class_<gem5::ruby::Switch, gem5::ruby::BasicRouter, std::unique_ptr<gem5::ruby::Switch, py::nodelete>>(m, "gem5_COLONS_ruby_COLONS_Switch")
        ;

}

static EmbeddedPyBind embed_obj("Switch", module_init, "BasicRouter");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummySwitchParamsClass
{
  public:
    gem5::ruby::Switch *create() const;
};

template <class CxxClass, class Enable=void>
class DummySwitchShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummySwitchShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const SwitchParams &>>>
{
  public:
    using Params = SwitchParams;
    static gem5::ruby::Switch *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummySwitchShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const SwitchParams &>>>
{
  public:
    using Params = DummySwitchParamsClass;
    static gem5::ruby::Switch *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ruby::Switch *
DummySwitchShunt<gem5::ruby::Switch>::Params::create() const
{
    return DummySwitchShunt<gem5::ruby::Switch>::create(*this);
}

} // namespace gem5
