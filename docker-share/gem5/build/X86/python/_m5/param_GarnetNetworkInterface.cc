/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/GarnetNetworkInterface.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/ruby/network/garnet/NetworkInterface.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_GarnetNetworkInterface");
    py::class_<GarnetNetworkInterfaceParams, ClockedObjectParams, std::unique_ptr<GarnetNetworkInterfaceParams, py::nodelete>>(m, "GarnetNetworkInterfaceParams")
        .def(py::init<>())
        .def("create", &GarnetNetworkInterfaceParams::create)
        .def_readwrite("garnet_deadlock_threshold", &GarnetNetworkInterfaceParams::garnet_deadlock_threshold)
        .def_readwrite("id", &GarnetNetworkInterfaceParams::id)
        .def_readwrite("vcs_per_vnet", &GarnetNetworkInterfaceParams::vcs_per_vnet)
        .def_readwrite("virt_nets", &GarnetNetworkInterfaceParams::virt_nets)
        ;

    py::class_<gem5::ruby::garnet::NetworkInterface, gem5::ClockedObject, std::unique_ptr<gem5::ruby::garnet::NetworkInterface, py::nodelete>>(m, "gem5_COLONS_ruby_COLONS_garnet_COLONS_NetworkInterface")
        ;

}

static EmbeddedPyBind embed_obj("GarnetNetworkInterface", module_init, "ClockedObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyGarnetNetworkInterfaceParamsClass
{
  public:
    gem5::ruby::garnet::NetworkInterface *create() const;
};

template <class CxxClass, class Enable=void>
class DummyGarnetNetworkInterfaceShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyGarnetNetworkInterfaceShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const GarnetNetworkInterfaceParams &>>>
{
  public:
    using Params = GarnetNetworkInterfaceParams;
    static gem5::ruby::garnet::NetworkInterface *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyGarnetNetworkInterfaceShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const GarnetNetworkInterfaceParams &>>>
{
  public:
    using Params = DummyGarnetNetworkInterfaceParamsClass;
    static gem5::ruby::garnet::NetworkInterface *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ruby::garnet::NetworkInterface *
DummyGarnetNetworkInterfaceShunt<gem5::ruby::garnet::NetworkInterface>::Params::create() const
{
    return DummyGarnetNetworkInterfaceShunt<gem5::ruby::garnet::NetworkInterface>::create(*this);
}

} // namespace gem5
