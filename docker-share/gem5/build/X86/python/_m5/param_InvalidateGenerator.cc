/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/InvalidateGenerator.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/testers/directedtest/InvalidateGenerator.hh"

#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_InvalidateGenerator");
    py::class_<InvalidateGeneratorParams, DirectedGeneratorParams, std::unique_ptr<InvalidateGeneratorParams, py::nodelete>>(m, "InvalidateGeneratorParams")
        .def(py::init<>())
        .def("create", &InvalidateGeneratorParams::create)
        .def_readwrite("addr_increment_size", &InvalidateGeneratorParams::addr_increment_size)
        ;

    py::class_<gem5::InvalidateGenerator, gem5::DirectedGenerator, std::unique_ptr<gem5::InvalidateGenerator, py::nodelete>>(m, "gem5_COLONS_InvalidateGenerator")
        ;

}

static EmbeddedPyBind embed_obj("InvalidateGenerator", module_init, "DirectedGenerator");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyInvalidateGeneratorParamsClass
{
  public:
    gem5::InvalidateGenerator *create() const;
};

template <class CxxClass, class Enable=void>
class DummyInvalidateGeneratorShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyInvalidateGeneratorShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const InvalidateGeneratorParams &>>>
{
  public:
    using Params = InvalidateGeneratorParams;
    static gem5::InvalidateGenerator *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyInvalidateGeneratorShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const InvalidateGeneratorParams &>>>
{
  public:
    using Params = DummyInvalidateGeneratorParamsClass;
    static gem5::InvalidateGenerator *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::InvalidateGenerator *
DummyInvalidateGeneratorShunt<gem5::InvalidateGenerator>::Params::create() const
{
    return DummyInvalidateGeneratorShunt<gem5::InvalidateGenerator>::create(*this);
}

} // namespace gem5
