/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/QoSTurnaroundPolicyIdeal.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/qos/turnaround_policy_ideal.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_QoSTurnaroundPolicyIdeal");
    py::class_<QoSTurnaroundPolicyIdealParams, QoSTurnaroundPolicyParams, std::unique_ptr<QoSTurnaroundPolicyIdealParams, py::nodelete>>(m, "QoSTurnaroundPolicyIdealParams")
        .def(py::init<>())
        .def("create", &QoSTurnaroundPolicyIdealParams::create)
        ;

    py::class_<gem5::memory::qos::TurnaroundPolicyIdeal, gem5::memory::qos::TurnaroundPolicy, std::unique_ptr<gem5::memory::qos::TurnaroundPolicyIdeal, py::nodelete>>(m, "gem5_COLONS_memory_COLONS_qos_COLONS_TurnaroundPolicyIdeal")
        ;

}

static EmbeddedPyBind embed_obj("QoSTurnaroundPolicyIdeal", module_init, "QoSTurnaroundPolicy");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyQoSTurnaroundPolicyIdealParamsClass
{
  public:
    gem5::memory::qos::TurnaroundPolicyIdeal *create() const;
};

template <class CxxClass, class Enable=void>
class DummyQoSTurnaroundPolicyIdealShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyQoSTurnaroundPolicyIdealShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const QoSTurnaroundPolicyIdealParams &>>>
{
  public:
    using Params = QoSTurnaroundPolicyIdealParams;
    static gem5::memory::qos::TurnaroundPolicyIdeal *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyQoSTurnaroundPolicyIdealShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const QoSTurnaroundPolicyIdealParams &>>>
{
  public:
    using Params = DummyQoSTurnaroundPolicyIdealParamsClass;
    static gem5::memory::qos::TurnaroundPolicyIdeal *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::memory::qos::TurnaroundPolicyIdeal *
DummyQoSTurnaroundPolicyIdealShunt<gem5::memory::qos::TurnaroundPolicyIdeal>::Params::create() const
{
    return DummyQoSTurnaroundPolicyIdealShunt<gem5::memory::qos::TurnaroundPolicyIdeal>::create(*this);
}

} // namespace gem5
