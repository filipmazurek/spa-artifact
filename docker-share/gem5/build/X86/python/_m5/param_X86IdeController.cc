/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/X86IdeController.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "dev/x86/ide_ctrl.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_X86IdeController");
    py::class_<X86IdeControllerParams, IdeControllerParams, std::unique_ptr<X86IdeControllerParams, py::nodelete>>(m, "X86IdeControllerParams")
        .def(py::init<>())
        .def("create", &X86IdeControllerParams::create)
        .def_readwrite("port_int_primary_connection_count", &X86IdeControllerParams::port_int_primary_connection_count)
        .def_readwrite("port_int_secondary_connection_count", &X86IdeControllerParams::port_int_secondary_connection_count)
        ;

    py::class_<gem5::X86IdeController, gem5::IdeController, std::unique_ptr<gem5::X86IdeController, py::nodelete>>(m, "gem5_COLONS_X86IdeController")
        ;

}

static EmbeddedPyBind embed_obj("X86IdeController", module_init, "IdeController");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyX86IdeControllerParamsClass
{
  public:
    gem5::X86IdeController *create() const;
};

template <class CxxClass, class Enable=void>
class DummyX86IdeControllerShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyX86IdeControllerShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const X86IdeControllerParams &>>>
{
  public:
    using Params = X86IdeControllerParams;
    static gem5::X86IdeController *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyX86IdeControllerShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const X86IdeControllerParams &>>>
{
  public:
    using Params = DummyX86IdeControllerParamsClass;
    static gem5::X86IdeController *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::X86IdeController *
DummyX86IdeControllerShunt<gem5::X86IdeController>::Params::create() const
{
    return DummyX86IdeControllerShunt<gem5::X86IdeController>::create(*this);
}

} // namespace gem5
