/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/X86NativeTrace.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/x86/nativetrace.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_X86NativeTrace");
    py::class_<X86NativeTraceParams, NativeTraceParams, std::unique_ptr<X86NativeTraceParams, py::nodelete>>(m, "X86NativeTraceParams")
        .def(py::init<>())
        .def("create", &X86NativeTraceParams::create)
        ;

    py::class_<gem5::trace::X86NativeTrace, gem5::trace::NativeTrace, std::unique_ptr<gem5::trace::X86NativeTrace, py::nodelete>>(m, "gem5_COLONS_trace_COLONS_X86NativeTrace")
        ;

}

static EmbeddedPyBind embed_obj("X86NativeTrace", module_init, "NativeTrace");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyX86NativeTraceParamsClass
{
  public:
    gem5::trace::X86NativeTrace *create() const;
};

template <class CxxClass, class Enable=void>
class DummyX86NativeTraceShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyX86NativeTraceShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const X86NativeTraceParams &>>>
{
  public:
    using Params = X86NativeTraceParams;
    static gem5::trace::X86NativeTrace *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyX86NativeTraceShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const X86NativeTraceParams &>>>
{
  public:
    using Params = DummyX86NativeTraceParamsClass;
    static gem5::trace::X86NativeTrace *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::trace::X86NativeTrace *
DummyX86NativeTraceShunt<gem5::trace::X86NativeTrace>::Params::create() const
{
    return DummyX86NativeTraceShunt<gem5::trace::X86NativeTrace>::create(*this);
}

} // namespace gem5
