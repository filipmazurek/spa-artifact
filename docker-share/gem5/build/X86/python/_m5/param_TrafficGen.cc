/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/TrafficGen.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/testers/traffic_gen/traffic_gen.hh"

#include <string>
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_TrafficGen");
    py::class_<TrafficGenParams, BaseTrafficGenParams, std::unique_ptr<TrafficGenParams, py::nodelete>>(m, "TrafficGenParams")
        .def(py::init<>())
        .def("create", &TrafficGenParams::create)
        .def_readwrite("config_file", &TrafficGenParams::config_file)
        ;

    py::class_<gem5::TrafficGen, gem5::BaseTrafficGen, std::unique_ptr<gem5::TrafficGen, py::nodelete>>(m, "gem5_COLONS_TrafficGen")
        ;

}

static EmbeddedPyBind embed_obj("TrafficGen", module_init, "BaseTrafficGen");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyTrafficGenParamsClass
{
  public:
    gem5::TrafficGen *create() const;
};

template <class CxxClass, class Enable=void>
class DummyTrafficGenShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyTrafficGenShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const TrafficGenParams &>>>
{
  public:
    using Params = TrafficGenParams;
    static gem5::TrafficGen *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyTrafficGenShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const TrafficGenParams &>>>
{
  public:
    using Params = DummyTrafficGenParamsClass;
    static gem5::TrafficGen *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::TrafficGen *
DummyTrafficGenShunt<gem5::TrafficGen>::Params::create() const
{
    return DummyTrafficGenShunt<gem5::TrafficGen>::create(*this);
}

} // namespace gem5
