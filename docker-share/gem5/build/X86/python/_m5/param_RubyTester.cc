/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/RubyTester.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/testers/rubytest/RubyTester.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "sim/system.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_RubyTester");
    py::class_<RubyTesterParams, ClockedObjectParams, std::unique_ptr<RubyTesterParams, py::nodelete>>(m, "RubyTesterParams")
        .def(py::init<>())
        .def("create", &RubyTesterParams::create)
        .def_readwrite("check_flush", &RubyTesterParams::check_flush)
        .def_readwrite("checks_to_complete", &RubyTesterParams::checks_to_complete)
        .def_readwrite("deadlock_threshold", &RubyTesterParams::deadlock_threshold)
        .def_readwrite("num_cpus", &RubyTesterParams::num_cpus)
        .def_readwrite("system", &RubyTesterParams::system)
        .def_readwrite("wakeup_frequency", &RubyTesterParams::wakeup_frequency)
        .def_readwrite("port_cpuInstDataPort_connection_count", &RubyTesterParams::port_cpuInstDataPort_connection_count)
        .def_readwrite("port_cpuInstPort_connection_count", &RubyTesterParams::port_cpuInstPort_connection_count)
        .def_readwrite("port_cpuDataPort_connection_count", &RubyTesterParams::port_cpuDataPort_connection_count)
        ;

    py::class_<gem5::RubyTester, gem5::ClockedObject, std::unique_ptr<gem5::RubyTester, py::nodelete>>(m, "gem5_COLONS_RubyTester")
        ;

}

static EmbeddedPyBind embed_obj("RubyTester", module_init, "ClockedObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyRubyTesterParamsClass
{
  public:
    gem5::RubyTester *create() const;
};

template <class CxxClass, class Enable=void>
class DummyRubyTesterShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyRubyTesterShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const RubyTesterParams &>>>
{
  public:
    using Params = RubyTesterParams;
    static gem5::RubyTester *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyRubyTesterShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const RubyTesterParams &>>>
{
  public:
    using Params = DummyRubyTesterParamsClass;
    static gem5::RubyTester *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::RubyTester *
DummyRubyTesterShunt<gem5::RubyTester>::Params::create() const
{
    return DummyRubyTesterShunt<gem5::RubyTester>::create(*this);
}

} // namespace gem5
