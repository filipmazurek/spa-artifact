/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/RubyDirectedTester.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/testers/directedtest/RubyDirectedTester.hh"

#include "cpu/testers/directedtest/DirectedGenerator.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_RubyDirectedTester");
    py::class_<RubyDirectedTesterParams, ClockedObjectParams, std::unique_ptr<RubyDirectedTesterParams, py::nodelete>>(m, "RubyDirectedTesterParams")
        .def(py::init<>())
        .def("create", &RubyDirectedTesterParams::create)
        .def_readwrite("generator", &RubyDirectedTesterParams::generator)
        .def_readwrite("requests_to_complete", &RubyDirectedTesterParams::requests_to_complete)
        .def_readwrite("port_cpuPort_connection_count", &RubyDirectedTesterParams::port_cpuPort_connection_count)
        ;

    py::class_<gem5::RubyDirectedTester, gem5::ClockedObject, std::unique_ptr<gem5::RubyDirectedTester, py::nodelete>>(m, "gem5_COLONS_RubyDirectedTester")
        ;

}

static EmbeddedPyBind embed_obj("RubyDirectedTester", module_init, "ClockedObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyRubyDirectedTesterParamsClass
{
  public:
    gem5::RubyDirectedTester *create() const;
};

template <class CxxClass, class Enable=void>
class DummyRubyDirectedTesterShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyRubyDirectedTesterShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const RubyDirectedTesterParams &>>>
{
  public:
    using Params = RubyDirectedTesterParams;
    static gem5::RubyDirectedTester *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyRubyDirectedTesterShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const RubyDirectedTesterParams &>>>
{
  public:
    using Params = DummyRubyDirectedTesterParamsClass;
    static gem5::RubyDirectedTester *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::RubyDirectedTester *
DummyRubyDirectedTesterShunt<gem5::RubyDirectedTester>::Params::create() const
{
    return DummyRubyDirectedTesterShunt<gem5::RubyDirectedTester>::create(*this);
}

} // namespace gem5
