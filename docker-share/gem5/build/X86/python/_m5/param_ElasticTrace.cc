/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/ElasticTrace.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/o3/probe/elastic_trace.hh"

#include <string>
#include "base/types.hh"
#include <string>
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_ElasticTrace");
    py::class_<ElasticTraceParams, ProbeListenerObjectParams, std::unique_ptr<ElasticTraceParams, py::nodelete>>(m, "ElasticTraceParams")
        .def(py::init<>())
        .def("create", &ElasticTraceParams::create)
        .def_readwrite("dataDepTraceFile", &ElasticTraceParams::dataDepTraceFile)
        .def_readwrite("depWindowSize", &ElasticTraceParams::depWindowSize)
        .def_readwrite("instFetchTraceFile", &ElasticTraceParams::instFetchTraceFile)
        .def_readwrite("startTraceInst", &ElasticTraceParams::startTraceInst)
        .def_readwrite("traceVirtAddr", &ElasticTraceParams::traceVirtAddr)
        ;

    py::class_<gem5::o3::ElasticTrace, gem5::ProbeListenerObject, std::unique_ptr<gem5::o3::ElasticTrace, py::nodelete>>(m, "gem5_COLONS_o3_COLONS_ElasticTrace")
        ;

}

static EmbeddedPyBind embed_obj("ElasticTrace", module_init, "ProbeListenerObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyElasticTraceParamsClass
{
  public:
    gem5::o3::ElasticTrace *create() const;
};

template <class CxxClass, class Enable=void>
class DummyElasticTraceShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyElasticTraceShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const ElasticTraceParams &>>>
{
  public:
    using Params = ElasticTraceParams;
    static gem5::o3::ElasticTrace *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyElasticTraceShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const ElasticTraceParams &>>>
{
  public:
    using Params = DummyElasticTraceParamsClass;
    static gem5::o3::ElasticTrace *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::o3::ElasticTrace *
DummyElasticTraceShunt<gem5::o3::ElasticTrace>::Params::create() const
{
    return DummyElasticTraceShunt<gem5::o3::ElasticTrace>::create(*this);
}

} // namespace gem5
