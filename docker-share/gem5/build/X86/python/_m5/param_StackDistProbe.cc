/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/StackDistProbe.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/probes/stack_dist.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "sim/system.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_StackDistProbe");
    py::class_<StackDistProbeParams, BaseMemProbeParams, std::unique_ptr<StackDistProbeParams, py::nodelete>>(m, "StackDistProbeParams")
        .def(py::init<>())
        .def("create", &StackDistProbeParams::create)
        .def_readwrite("disable_linear_hists", &StackDistProbeParams::disable_linear_hists)
        .def_readwrite("disable_log_hists", &StackDistProbeParams::disable_log_hists)
        .def_readwrite("line_size", &StackDistProbeParams::line_size)
        .def_readwrite("linear_hist_bins", &StackDistProbeParams::linear_hist_bins)
        .def_readwrite("log_hist_bins", &StackDistProbeParams::log_hist_bins)
        .def_readwrite("system", &StackDistProbeParams::system)
        .def_readwrite("verify", &StackDistProbeParams::verify)
        ;

    py::class_<gem5::StackDistProbe, gem5::BaseMemProbe, std::unique_ptr<gem5::StackDistProbe, py::nodelete>>(m, "gem5_COLONS_StackDistProbe")
        ;

}

static EmbeddedPyBind embed_obj("StackDistProbe", module_init, "BaseMemProbe");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyStackDistProbeParamsClass
{
  public:
    gem5::StackDistProbe *create() const;
};

template <class CxxClass, class Enable=void>
class DummyStackDistProbeShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyStackDistProbeShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const StackDistProbeParams &>>>
{
  public:
    using Params = StackDistProbeParams;
    static gem5::StackDistProbe *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyStackDistProbeShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const StackDistProbeParams &>>>
{
  public:
    using Params = DummyStackDistProbeParamsClass;
    static gem5::StackDistProbe *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::StackDistProbe *
DummyStackDistProbeShunt<gem5::StackDistProbe>::Params::create() const
{
    return DummyStackDistProbeShunt<gem5::StackDistProbe>::create(*this);
}

} // namespace gem5
