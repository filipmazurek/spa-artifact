/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/BloomFilterBulk.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "base/filters/bulk_bloom_filter.hh"

namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_BloomFilterBulk");
    py::class_<BloomFilterBulkParams, BloomFilterMultiBitSelParams, std::unique_ptr<BloomFilterBulkParams, py::nodelete>>(m, "BloomFilterBulkParams")
        .def(py::init<>())
        .def("create", &BloomFilterBulkParams::create)
        ;

    py::class_<gem5::bloom_filter::Bulk, gem5::bloom_filter::MultiBitSel, std::unique_ptr<gem5::bloom_filter::Bulk, py::nodelete>>(m, "gem5_COLONS_bloom_filter_COLONS_Bulk")
        ;

}

static EmbeddedPyBind embed_obj("BloomFilterBulk", module_init, "BloomFilterMultiBitSel");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyBloomFilterBulkParamsClass
{
  public:
    gem5::bloom_filter::Bulk *create() const;
};

template <class CxxClass, class Enable=void>
class DummyBloomFilterBulkShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyBloomFilterBulkShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const BloomFilterBulkParams &>>>
{
  public:
    using Params = BloomFilterBulkParams;
    static gem5::bloom_filter::Bulk *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyBloomFilterBulkShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const BloomFilterBulkParams &>>>
{
  public:
    using Params = DummyBloomFilterBulkParamsClass;
    static gem5::bloom_filter::Bulk *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::bloom_filter::Bulk *
DummyBloomFilterBulkShunt<gem5::bloom_filter::Bulk>::Params::create() const
{
    return DummyBloomFilterBulkShunt<gem5::bloom_filter::Bulk>::create(*this);
}

} // namespace gem5
