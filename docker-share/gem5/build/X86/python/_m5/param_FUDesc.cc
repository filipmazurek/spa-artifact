/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/FUDesc.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "cpu/func_unit.hh"

#include "base/types.hh"
#include <vector>
#include "cpu/func_unit.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_FUDesc");
    py::class_<FUDescParams, SimObjectParams, std::unique_ptr<FUDescParams, py::nodelete>>(m, "FUDescParams")
        .def(py::init<>())
        .def("create", &FUDescParams::create)
        .def_readwrite("count", &FUDescParams::count)
        .def_readwrite("opList", &FUDescParams::opList)
        ;

    py::class_<gem5::FUDesc, gem5::SimObject, std::unique_ptr<gem5::FUDesc, py::nodelete>>(m, "gem5_COLONS_FUDesc")
        ;

}

static EmbeddedPyBind embed_obj("FUDesc", module_init, "SimObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyFUDescParamsClass
{
  public:
    gem5::FUDesc *create() const;
};

template <class CxxClass, class Enable=void>
class DummyFUDescShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyFUDescShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const FUDescParams &>>>
{
  public:
    using Params = FUDescParams;
    static gem5::FUDesc *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyFUDescShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const FUDescParams &>>>
{
  public:
    using Params = DummyFUDescParamsClass;
    static gem5::FUDesc *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::FUDesc *
DummyFUDescShunt<gem5::FUDesc>::Params::create() const
{
    return DummyFUDescShunt<gem5::FUDesc>::create(*this);
}

} // namespace gem5
