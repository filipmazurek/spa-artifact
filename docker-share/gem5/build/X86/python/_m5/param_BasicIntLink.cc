/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/BasicIntLink.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/ruby/network/BasicLink.hh"

#include <string>
#include "mem/ruby/network/BasicRouter.hh"
#include "mem/ruby/network/BasicRouter.hh"
#include <string>
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_BasicIntLink");
    py::class_<BasicIntLinkParams, BasicLinkParams, std::unique_ptr<BasicIntLinkParams, py::nodelete>>(m, "BasicIntLinkParams")
        .def(py::init<>())
        .def("create", &BasicIntLinkParams::create)
        .def_readwrite("dst_inport", &BasicIntLinkParams::dst_inport)
        .def_readwrite("dst_node", &BasicIntLinkParams::dst_node)
        .def_readwrite("src_node", &BasicIntLinkParams::src_node)
        .def_readwrite("src_outport", &BasicIntLinkParams::src_outport)
        ;

    py::class_<gem5::ruby::BasicIntLink, gem5::ruby::BasicLink, std::unique_ptr<gem5::ruby::BasicIntLink, py::nodelete>>(m, "gem5_COLONS_ruby_COLONS_BasicIntLink")
        ;

}

static EmbeddedPyBind embed_obj("BasicIntLink", module_init, "BasicLink");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyBasicIntLinkParamsClass
{
  public:
    gem5::ruby::BasicIntLink *create() const;
};

template <class CxxClass, class Enable=void>
class DummyBasicIntLinkShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyBasicIntLinkShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const BasicIntLinkParams &>>>
{
  public:
    using Params = BasicIntLinkParams;
    static gem5::ruby::BasicIntLink *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyBasicIntLinkShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const BasicIntLinkParams &>>>
{
  public:
    using Params = DummyBasicIntLinkParamsClass;
    static gem5::ruby::BasicIntLink *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ruby::BasicIntLink *
DummyBasicIntLinkShunt<gem5::ruby::BasicIntLink>::Params::create() const
{
    return DummyBasicIntLinkShunt<gem5::ruby::BasicIntLink>::create(*this);
}

} // namespace gem5
