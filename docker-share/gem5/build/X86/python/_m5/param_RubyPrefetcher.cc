/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/RubyPrefetcher.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/ruby/structures/RubyPrefetcher.hh"

#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_RubyPrefetcher");
    py::class_<RubyPrefetcherParams, SimObjectParams, std::unique_ptr<RubyPrefetcherParams, py::nodelete>>(m, "RubyPrefetcherParams")
        .def(py::init<>())
        .def("create", &RubyPrefetcherParams::create)
        .def_readwrite("cross_page", &RubyPrefetcherParams::cross_page)
        .def_readwrite("nonunit_filter", &RubyPrefetcherParams::nonunit_filter)
        .def_readwrite("num_startup_pfs", &RubyPrefetcherParams::num_startup_pfs)
        .def_readwrite("num_streams", &RubyPrefetcherParams::num_streams)
        .def_readwrite("page_shift", &RubyPrefetcherParams::page_shift)
        .def_readwrite("train_misses", &RubyPrefetcherParams::train_misses)
        .def_readwrite("unit_filter", &RubyPrefetcherParams::unit_filter)
        ;

    py::class_<gem5::ruby::RubyPrefetcher, gem5::SimObject, std::unique_ptr<gem5::ruby::RubyPrefetcher, py::nodelete>>(m, "gem5_COLONS_ruby_COLONS_RubyPrefetcher")
        ;

}

static EmbeddedPyBind embed_obj("RubyPrefetcher", module_init, "SimObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyRubyPrefetcherParamsClass
{
  public:
    gem5::ruby::RubyPrefetcher *create() const;
};

template <class CxxClass, class Enable=void>
class DummyRubyPrefetcherShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyRubyPrefetcherShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const RubyPrefetcherParams &>>>
{
  public:
    using Params = RubyPrefetcherParams;
    static gem5::ruby::RubyPrefetcher *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyRubyPrefetcherShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const RubyPrefetcherParams &>>>
{
  public:
    using Params = DummyRubyPrefetcherParamsClass;
    static gem5::ruby::RubyPrefetcher *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ruby::RubyPrefetcher *
DummyRubyPrefetcherShunt<gem5::ruby::RubyPrefetcher>::Params::create() const
{
    return DummyRubyPrefetcherShunt<gem5::ruby::RubyPrefetcher>::create(*this);
}

} // namespace gem5
