/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/NetworkLink.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "mem/ruby/network/garnet/NetworkLink.hh"

#include "base/types.hh"
#include "base/types.hh"
#include <vector>
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
#include "base/types.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_NetworkLink");
    py::class_<NetworkLinkParams, ClockedObjectParams, std::unique_ptr<NetworkLinkParams, py::nodelete>>(m, "NetworkLinkParams")
        .def(py::init<>())
        .def("create", &NetworkLinkParams::create)
        .def_readwrite("link_id", &NetworkLinkParams::link_id)
        .def_readwrite("link_latency", &NetworkLinkParams::link_latency)
        .def_readwrite("supported_vnets", &NetworkLinkParams::supported_vnets)
        .def_readwrite("vcs_per_vnet", &NetworkLinkParams::vcs_per_vnet)
        .def_readwrite("virt_nets", &NetworkLinkParams::virt_nets)
        .def_readwrite("width", &NetworkLinkParams::width)
        ;

    py::class_<gem5::ruby::garnet::NetworkLink, gem5::ClockedObject, std::unique_ptr<gem5::ruby::garnet::NetworkLink, py::nodelete>>(m, "gem5_COLONS_ruby_COLONS_garnet_COLONS_NetworkLink")
        ;

}

static EmbeddedPyBind embed_obj("NetworkLink", module_init, "ClockedObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyNetworkLinkParamsClass
{
  public:
    gem5::ruby::garnet::NetworkLink *create() const;
};

template <class CxxClass, class Enable=void>
class DummyNetworkLinkShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyNetworkLinkShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const NetworkLinkParams &>>>
{
  public:
    using Params = NetworkLinkParams;
    static gem5::ruby::garnet::NetworkLink *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyNetworkLinkShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const NetworkLinkParams &>>>
{
  public:
    using Params = DummyNetworkLinkParamsClass;
    static gem5::ruby::garnet::NetworkLink *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::ruby::garnet::NetworkLink *
DummyNetworkLinkShunt<gem5::ruby::garnet::NetworkLink>::Params::create() const
{
    return DummyNetworkLinkShunt<gem5::ruby::garnet::NetworkLink>::create(*this);
}

} // namespace gem5
