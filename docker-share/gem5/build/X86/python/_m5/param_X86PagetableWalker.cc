/**
 * DO NOT EDIT THIS FILE!
 * File automatically generated by
 *   build_tools/sim_object_param_struct_cc.py:297
 */

#include "pybind11/pybind11.h"
#include "pybind11/stl.h"

#include <type_traits>

#include "base/compiler.hh"
#include "params/X86PagetableWalker.hh"
#include "sim/init.hh"
#include "sim/sim_object.hh"

#include "arch/x86/pagetable_walker.hh"

#include "base/types.hh"
#include "sim/system.hh"
namespace py = pybind11;

namespace gem5
{

static void
module_init(py::module_ &m_internal)
{
py::module_ m = m_internal.def_submodule("param_X86PagetableWalker");
    py::class_<X86PagetableWalkerParams, ClockedObjectParams, std::unique_ptr<X86PagetableWalkerParams, py::nodelete>>(m, "X86PagetableWalkerParams")
        .def(py::init<>())
        .def("create", &X86PagetableWalkerParams::create)
        .def_readwrite("num_squash_per_cycle", &X86PagetableWalkerParams::num_squash_per_cycle)
        .def_readwrite("system", &X86PagetableWalkerParams::system)
        .def_readwrite("port_port_connection_count", &X86PagetableWalkerParams::port_port_connection_count)
        ;

    py::class_<gem5::X86ISA::Walker, gem5::ClockedObject, std::unique_ptr<gem5::X86ISA::Walker, py::nodelete>>(m, "gem5_COLONS_X86ISA_COLONS_Walker")
        ;

}

static EmbeddedPyBind embed_obj("X86PagetableWalker", module_init, "ClockedObject");

} // namespace gem5
namespace gem5
{

namespace
{

/*
 * If we can't define a default create() method for this params
 * struct because the SimObject doesn't have the right
 * constructor, use template magic to make it so we're actually
 * defining a create method for this class instead.
 */
class DummyX86PagetableWalkerParamsClass
{
  public:
    gem5::X86ISA::Walker *create() const;
};

template <class CxxClass, class Enable=void>
class DummyX86PagetableWalkerShunt;

/*
 * This version directs to the real Params struct and the
 * default behavior of create if there's an appropriate
 * constructor.
 */
template <class CxxClass>
class DummyX86PagetableWalkerShunt<CxxClass, std::enable_if_t<
    std::is_constructible_v<CxxClass, const X86PagetableWalkerParams &>>>
{
  public:
    using Params = X86PagetableWalkerParams;
    static gem5::X86ISA::Walker *
    create(const Params &p)
    {
        return new CxxClass(p);
    }
};

/*
 * This version diverts to the DummyParamsClass and a dummy
 * implementation of create if the appropriate constructor does
 * not exist.
 */
template <class CxxClass>
class DummyX86PagetableWalkerShunt<CxxClass, std::enable_if_t<
    !std::is_constructible_v<CxxClass, const X86PagetableWalkerParams &>>>
{
  public:
    using Params = DummyX86PagetableWalkerParamsClass;
    static gem5::X86ISA::Walker *
    create(const Params &p)
    {
        return nullptr;
    }
};

} // anonymous namespace

/*
 * An implementation of either the real Params struct's create
 * method, or the Dummy one. Either an implementation is
 * mandantory since this was shunted off to the dummy class, or
 * one is optional which will override this weak version.
 */
[[maybe_unused]] gem5::X86ISA::Walker *
DummyX86PagetableWalkerShunt<gem5::X86ISA::Walker>::Params::create() const
{
    return DummyX86PagetableWalkerShunt<gem5::X86ISA::Walker>::create(*this);
}

} // namespace gem5
